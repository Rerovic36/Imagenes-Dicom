Servidor en express y Node JS para relacionar información de una base de datos con un servidor dicom para descargar imágenes usando el ID del paciente

Ejecutar npm install para instalar las dependencias y npm run start para ejecutar la aplicacion


Express server and Node JS to link database information with a DICOM server for downloading images using the patient ID

Run npm install to install the dependencies and npm run start to run the application